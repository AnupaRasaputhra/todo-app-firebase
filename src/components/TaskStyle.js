import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  task: {
    marginBottom: 10,
    backgroundColor: '#f9f9f9',
    borderRadius: 5,
    padding: 10,
    elevation: 3,
  },
  trashIcon: {
    color: 'red',
  },
});
